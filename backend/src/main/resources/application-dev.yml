spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookshelf
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  config:
    import: optional:file:../.env[.properties]

app:
  api-key: ${API_KEY}
  frontend-url: ${FRONTEND_URL}:5173
  demo-shelf-public-id: ${DEMO_BOOKSHELF_PUBLIC_ID}
  seed-demo-shelf: true

logging:
  level:
    io.github.tyostokarry.bookshelf: DEBUG
